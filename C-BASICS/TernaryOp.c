#include <stdio.h>
int main()
{
    int a = 10 > 7 ? 1 : 0; /*the condition of ternary operand is that 10>7 true or not,
            if it is true then it will print 1,
            if not it will print 0 */
                            // the synatx must be like

    /*int varabile = values comparison ex:(3>1) ? (the that should be assigned to varabile : or not) ex:(3:4)*/
    printf("%d", a);
    return 0;
}