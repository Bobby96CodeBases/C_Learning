#include <stdio.h>
int main()
{
    int i;
    while (i != 22)
    {
        for (i = 11; i <= 21; i++) /*while loop only executes finte loop only when given statement is true;
         otherwise it does not respond  */
        {
            printf("\nHV");
        }
    }
}